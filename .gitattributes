# 部署脚本强制 LF 行尾，避免 Windows 提交后在 macOS/Linux 上报 bad interpreter
*.sh text eol=lf
*.cjs text eol=lf
*.ps1 text eol=lf

# Windows 批处理必须 CRLF：LF 行尾会让 cmd.exe 解析异常
# （goto 找不到标签、脚本中途退出，且没有任何报错信息）
*.bat text eol=crlf
*.cmd text eol=crlf
