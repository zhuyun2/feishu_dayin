# 依赖与构建产物
node_modules/
dist/
*.log

# 本地开发/调试配置（浏览器预览工具生成，与部署无关）
.claude/

# 保留模板库与匹配配置（本地部署直接调用）
!templates/
!templates/**

# pm2 进程配置（deploy.sh 按本地端口生成，不提交）
ecosystem.config.cjs

# 内网穿透临时地址（cloudflared 每次重启变化，不入库）
tunnel-url.txt
.run/

# 一键部署的现场日志与本机 node 路径（因机器而异，不入库）
deploy-log.txt
node-path.txt
